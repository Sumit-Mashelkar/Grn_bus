# =========================================================================
# TransitPulse — Backend dependencies (Flask + SQLite + Socket.IO)
# =========================================================================
# This file lists ONLY the direct runtime dependencies.  Pip resolves all
# transitive packages (werkzeug, jinja2, click, blinker, itsdangerous,
# python-engineio, bidict, simple-websocket, h11, wsproto, etc.) from these.
# SQLite is in the Python standard library — no extra package required.
# -------------------------------------------------------------------------

# Web framework
Flask==3.1.3
Flask-Cors==6.0.2

# Real-time WebSocket layer (ASGI mode, used with uvicorn)
python-socketio==5.16.1

# WSGI → ASGI shim so the Flask WSGI app can run under uvicorn
asgiref==3.11.1

# ASGI server (the platform's supervisor invokes: `uvicorn server:app`)
uvicorn==0.25.0

# .env loader
python-dotenv==1.2.2
